# 制約

- 拡張機能は Manifest V3 として実装する
- 保存対象は現在アクティブなタブの URL とタイトルを基本とする
- API サーバー URL はユーザーがポップアップで変更でき、WebExtension の `browser.storage.local` に保存される
- API サーバー URL の初期値は `http://localhost:8000` である
- フォルダとタグは API 側の既存データを参照して選択する
- タグは複数選択、フォルダは単一選択とする
- API 接続成功時は、現在タブを接続先ごとに一度だけ自動保存しようとする
- Reload による同一接続先の再確認では、自動保存、フォルダ取得、タグ取得を繰り返さない
- background worker と content script は使用せず、Web ページへコードを注入しない
- 接続成功時は、既存ブックマークを URL で読み込んでフォームへ反映する
- 保存時は既存ブックマークの更新を優先し、`404` の場合のみ新規作成にフォールバックする
- 削除は URL ベースで行い、保存済みブックマークがなければ失敗扱いにする
- API 接続失敗時はステータス表示で明示する
