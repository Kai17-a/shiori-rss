# Frontend E2E Observations

Playwright verifies that the app root directly renders the feed library, the old `/feeds` and `/rss` routes are absent, removed navigation and custom-source UI stay absent, and settings expose Automation and LLM through category tabs.
