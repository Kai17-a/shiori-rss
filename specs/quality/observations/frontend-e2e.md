# Frontend E2E Observations

Playwright verifies that the app root opens `/feeds`, the old `/rss` route is absent, removed navigation and custom-source UI stay absent, and settings expose Automation and LLM through category tabs.
