# Frontend E2E Observations

Playwright verifies that `/` renders the rolling 24-hour dashboard without promotional copy, `/feeds` and `/custom-feeds` render their libraries, the sidebar lists and folds custom RSS sources, the old `/rss` route stays absent, Ask AI opens and streams Markdown with cited sources, asynchronous controls expose loading states, and settings expose Automation and LLM through category tabs without a hero.
